name: Push

on:
    push:
        branches: [develop, main]

permissions:
    contents: read

jobs:
    test:
        uses: ./.github/workflows/_test.yml
        secrets:
            CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
