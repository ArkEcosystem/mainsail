syntax = "proto3";

package getMessages;

message GetMessagesRequest {
    shared.Headers headers = 1;
}

message GetMessagesResponse {
    repeated bytes prevotes = 1;
    repeated bytes precommits = 2;
}
